.x-dashboard
  .x-dashboard__avatar
    = gravatar_for @user
  %ul.x-dashboard__info
    %li.x-dashboard__info-item
      = t('.name')
      =':'
      = @user.name

    %li.x-dashboard__info-item
      = t('.email')
      =':'
      = @user.email

= link_to t('.start'), work_path, class: "x-button"
